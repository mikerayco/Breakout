//! tiny-skia drawing wrappers (ADR-0005).
//!
//! Owns the rasteriser calls: shapes, fills, anti-aliasing, additive blending
//! for the game's pixel scene. Must never mutate simulation state (Phase 1).

pub fn stub() -> ! {
    todo!("render/draw: implemented in Phase 1")
}
