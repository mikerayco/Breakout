//! Named colours from MOCKUP.md §2 (Phase 4 locks them in verbatim).
//!
//! Owns every colour constant; no magic numbers outside `tuning.rs` and
//! here (AGENTS §3). Must not encode game rules.

pub fn stub() -> ! {
    todo!("render/palette: implemented in Phase 4")
}
