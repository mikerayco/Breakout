//! The RGB framebuffer the game draws into (ADR-0002, ADR-0003).
//!
//! Owns the `320·S × 240·S` pixel buffer and its `clear`/`pixel_mut`/
//! `as_bytes` surface. Must not know anything about game rules (Phase 1).

pub fn stub() -> ! {
    todo!("render/framebuffer: implemented in Phase 1")
}
