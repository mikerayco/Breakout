{
  "id": "0eb19553",
  "title": "Phase 2 — The core loop: paddle, ball, bricks, lives",
  "tags": [
    "phase-2",
    "plan",
    "physics",
    "core-loop"
  ],
  "status": "open",
  "created_at": "2026-08-29T09:27:01.354Z"
}

## Goal\nA boring but correct Breakout. Correctness first, feel later.\n\n## Status (2026-09-03): implementation complete, automated gates GREEN.\n- tuning.rs (Mike-approved values), state.rs, physics.rs (swept TOI, english+clamp, steel/explosive, scoring/lives/combo/ramp), rng.rs, score.rs, level.rs hardcoded level, draw_world flat rects+circle, main.rs fixed-timestep loop with Title/Playing/Paused/Clear/Over.\n- `cargo test`: 18 passed (tunnelling, corner, clamp, explosive chain, steel-clear, combo, determinism, paddle clamp, 10-min soak, state, score, shm, font, card).\n- `cargo clippy --all-targets -- -D warnings`: clean. `cargo fmt --check`: clean. `cargo build --release`: clean. `--caps`: works.\n- `--seed 42` determinism proven by deterministic_replay_is_stable test (headless replay harness, OQ-7).\n\n## Still needs Mike (stop rule + Ghostty gates)\n- 10 min continuous play, no ball escape/panic (headless 10-min soak passes).\n- Clear one level; lose 3 balls ends run; no horizontal loop (asserted in soak).\n- Phase 0/1 Ghostty gates in PERF.md still unchecked (p99 numbers, resize, transports, panic/SIGINT paths).\n\n## Runnable outcome\nA single hard-coded level you can actually clear or lose.\n\n## Tasks (PLAN Phase 2)\n1. `src/game/tuning.rs` — DONE (approved values).\n2. `src/game/state.rs` — DONE.\n3. `src/game/physics.rs` — DONE.\n4. Fixed-timestep 240 Hz — DONE (accumulator, MAX_CATCHUP 5, spiral guard).\n5. Brick grid/scoring/lives/combo/ramp — DONE.\n6. Flat rects + circle — DONE (draw_world).\n7. `src/game/rng.rs` — DONE.\n8. Unit tests — DONE (18 tests).\n\n## Test gate\n```sh\ncargo test\ncargo run --release\ncargo run --release -- --seed 42     # twice; identical brick drops and outcomes\n```\n\n## Stop rule\nTen minutes of continuous play with no ball escape and no panic.\n\n## Not in this phase\njuice, powerups, level files, audio, perks, save data.
