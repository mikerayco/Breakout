{
  "id": "a6652e6a",
  "title": "Propose complete tuning.rs for Mike's review (blocks Phase 2)",
  "tags": [
    "phase-2",
    "plan",
    "tuning",
    "review-gated"
  ],
  "status": "open",
  "created_at": "2026-08-29T09:26:52.631Z"
}

## Review-gated deliverable owed to Mike before Phase 2 builds.

Per PRD §8 "Additional clarifications" (2026-08-29): the agent proposes a complete `tuning.rs` for Mike's review; Mike approves before Phase 2 implementation.

## Scope of the proposal
A complete `tuning.rs` with EVERY numeric constant for the simulation, documented units (PRD FR-22). Includes (at minimum):
- Ball base speed, speed ramp (per bricks destroyed + per level index), hard cap.
- Paddle acceleration, friction, momentum carried into ball on contact (FR-13).
- Paddle reflection english + minimum-vertical-angle clamp (FR-15).
- Brick HP 1–5, steel, explosive 3×3 (FR-17/18/19); scoring base × combo (FR-23).
- Lives, combo reset rules.
- Multiball hard cap = 8 (resolved 2026-08-29).
- Fixed timestep DT = 1/240, MAX_CATCHUP = 5 (ADR-0006).

Numbers outside `tuning.rs`/`palette.rs` are forbidden (AGENTS §3). Values from MOCKUP §4 (juice/effect specs) go in their respective render modules / `palette.rs`, not here unless they affect simulation.

## Constraint
AGENTS §0 rule 2: do not choose a tuning value on your own. This proposal is the exception path — propose, then wait for Mike's approval before building Phase 2.

## Done when
Mike approves the proposed `tuning.rs` (or returns it with changes).
